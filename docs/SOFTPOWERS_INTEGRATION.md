# Softpowers integration

Status: superseded as an operator procedure by
[the current Softpowers case study](../case-studies/softpowers.md).

This stable route remains for old links. Current v0.2 does not ship a
`softpowers-companion/` tree, does not read a v1 pack through normal commands,
and does not copy Field Lab runtime bytes into Softpowers. Use a disposable
external lab or the one-shot migrator, then run canonical v2 commands.
