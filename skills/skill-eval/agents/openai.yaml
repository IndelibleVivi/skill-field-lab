interface:
  display_name: "Field Trial"
  short_description: "Find the weakest sufficient evidence path"
  default_prompt: "Use $skill-eval to test whether existing work already resolves this one behavior claim. If not, design the smallest defensible no-spend plan and enumerate every target invocation before any live run."
policy:
  products:
    - CODEX
  allow_implicit_invocation: false
