interface:
  display_name: "Pattern Intake"
  short_description: "Adapt external mechanisms without importing the machinery"
  default_prompt: "Use $pattern-intake to pin this source, distill one portable mechanism, compare it with the local system, and close with an evidence-bounded decision. A no-change result is valid; do not create a lab or live run unless one unresolved claim genuinely needs it."
policy:
  products:
    - CODEX
  allow_implicit_invocation: true
